/* Write a SQL query to list employee names and department names where the 
department is in 'Chicago' using INNER JOIN.

+-------+-------+
| ename | dname |
+-------+-------+
*/
USE test; 
select e.ename, d.dname
from emp e 
join dept d on d.deptno=e.deptno
where d.location in ("Chicago");